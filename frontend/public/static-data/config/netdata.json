{
  "name": "netdata",
  "mcpServers": {
    "netdata": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-netdata"
      ]
    }
  },
  "commands": {
    "npx": [
      "-y",
      "@modelcontextprotocol/server-netdata"
    ]
  },
  "install": {
    "npm": "npx -y @modelcontextprotocol/server-netdata"
  }
}