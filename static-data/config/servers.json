{
  "name": "servers",
  "mcpServers": {
    "servers": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-servers"
      ]
    }
  },
  "commands": {
    "npx": [
      "-y",
      "@modelcontextprotocol/server-servers"
    ]
  },
  "install": {
    "npm": "npx -y @modelcontextprotocol/server-servers"
  }
}